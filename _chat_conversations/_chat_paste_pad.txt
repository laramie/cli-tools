Cool.  Just to be sure.  I'm proposing this structure: 

table-builder.js ::
    export function findTuning(oneBaseID)
    export function findTuningForName
    export function bindFormTuningsEvents()  // calls jQuery.  Should be encapsulated so it doesn't get called from headless _tests

TableBuilder.js ::
     //imports pretty much all functions from table-builder.js
     export function findTuning ....        // only on the "public" api
     export function findTuningForName .... // only on the "public" api

 infinite-neck.js ::
     imports only 
        TableBuilder.findTuning 
        TableBuilder.findTuningForName    
