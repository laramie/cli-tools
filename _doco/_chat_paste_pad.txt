
sections[3],noteTables[tblUSER_copy:3,tblBanjo:2,tbleGuitar:4],namedNotes:2

OK.  I did some work, and fixed some things, but messed up other things!  
I need you to help me sort it out.  
I put the two describe() calls at the end of the file.  
The first one, describe('Song Section count'...) is working.  
The second one, describe('Song Section Structure and Summary'...) is not working because of error: 

  TypeError: The "path" argument must be of type string. Received an instance of Object
  
The line of code is shown in the test output of the stack trace, where the caret points to the .join() call:

> 189 |       const songPath = path.join(SONGS_DIR, songFile);

This is, no doubt, caused by me trying to reuse songFiles where before you had something else.

So this line of the file (as it is in the workspace now) works, because it uses the object set up the way it worked for the earlier tests you wrote:   

   songFiles.forEach(({ file, expectedFailure }) => { .......

I prefer this because it uses the songs in the song-list.json file rather than a directory scan.
Whereas this line does not work:

   for (const songFile of songFiles) { .......

because it was expecting what you did before:

  const SONGS_DIR = path.join(__dirname, '../../songs');
  const songFiles = fs.readdirSync(SONGS_DIR).filter(f => f.endsWith('.json'));

  for (const songFile of songFiles) {
    test(`sections summary for ${songFile}`, () => {
      and so on 

So I want you to help me fix 

    describe('Song Section Structure and Summary', () => { ......

to follow the songFiles strategy of 

    describe('Song Section count', () => { ......

 