// Lightweight test helper barrel for Song-focused Jest files.
// Not currently used in song tests, but perhaps useful when setting up more song tests. to be evaluated again after those tests are implemented.
export { makeSong } from '../../song.js';
export {
    setupSongTests,
    getSong,
    getCurrentSection,
    getSectionsCurrentIndex,
    getSections
} from '../../infinite-neck.js';

